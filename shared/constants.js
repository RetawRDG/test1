// Shared battle constants to keep gameplay values consistent across the app.
const ATTACK_ZONES = ["head", "body", "arms", "legs"];

// Base amount of damage dealt on a successful hit.
const BASE_DAMAGE = 10;

module.exports = {
  ATTACK_ZONES,
  BASE_DAMAGE,
};
